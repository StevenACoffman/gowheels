name: Post-Release

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  pypi-publish:
    name: Publish to PyPI
    environment: pypi
    permissions:
      id-token: write
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install gowheels
        run: go install github.com/StevenACoffman/gowheels@${{ github.event.release.tag_name }}

      - name: Build wheels and publish to PyPI
        run: gowheels pypi --name gowheels --repo ${{ github.repository }} --upload
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
